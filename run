#!/bin/bash
echo "--------------------------------------\n"
echo "You can run this "
mkdir -p /tmp/localdata

/cbfs/cbfs -nodeID=$MYNODEID \
       -bucket=cbfs \
       -couchbase=http://$CBSERVER:8091/
       -root=/tmp/localdata \
       -viewProxy" &
       
/cbfs/cbfsclient http://localhost:8484/ upload \
    /cbfs/monitor monitor
